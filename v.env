# Environment variables for Property Management App
NODE_ENV=production
PORT=3000

# Secret key for JWT session management (Change this in production!)
JWT_SECRET=shinigami_secret_key_change_me_in_production

# Path to the SQLite database
DATABASE_URL=/app/data/shinigami.db
